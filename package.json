{
  "name": "byrock-story-explorer",
  "version": "0.1.0",
  "private": true,
  "description": "A deterministic, accessible branching story experience for Byrock.",
  "type": "module",
  "scripts": {
    "start": "node src/server.mjs",
    "dev": "node --watch src/server.mjs",
    "seed": "node scripts/seed.mjs",
    "check": "node --check src/server.mjs && node --check scripts/seed.mjs"
  },
  "engines": { "node": ">=20" }
}
